"use client";

const useAuth = () => {
  return {
    isPending: false,
    setValue: () => {},
    onAuth: () => {},
    watch: () => {},
    register: () => {},
    errors: {},
  };
};

export default useAuth;
